#! /usr/bin/env node

// Traversy Media:
// https://www.youtube.com/watch?v=u4URamXstM0

"use strict";

let add = function( a, b ) {
    let sum = a + b;
    console.log(sum);
    return false;
};

add(2, 2);



let addArrow = ( a, b ) => {
    let sum = a + b;
    console.log(sum);
    return false;
};

addArrow(2, 2)

// OK Not much of a tutorial at all. Normally his tuts are good, but only the first part of this one is
// of any value. See 01arrow... and 02arrow...

